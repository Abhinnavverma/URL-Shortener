# Go URL Shortener

A high-performance URL shortener built with Go, PostgreSQL, and Docker.

## 🚀 Tech Stack
- **Language:** Go (Golang)
- **Database:** PostgreSQL
- **Infrastructure:** Docker & Docker Compose
- **Routing:** Chi Router
- **Architecture:** Interface-based Dependency Injection

## 🛠️ How to Run
1. Clone the repo
2. Run `docker-compose up -d` to start the DB.
3. Run `go run cmd/api/main.go`